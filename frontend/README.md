# Frontend

React (Vite) frontend for the Grievance Redressal Portal.

- Place React components in `components/`
- Main pages in `pages/`
- Client-side services in `services/`
- Main entry: `App.tsx`, `index.tsx`
- Frontend config: `package.json`, `vite.config.ts`, `tsconfig.json`
